server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/MemberDatabase?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 2345s
  thymeleaf:
    cache: false

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    generate-ddl: true